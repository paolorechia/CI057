#include <stdio.h>
#include <stdlib.h>

typedef struct bst {
	int chave;
	char letra;
	struct bst * pai;
	struct bst * esq;
	struct bst * dir;
} Nodo;

Nodo * insere( Nodo * nodo, int chave, char letra){



int main(){
	Nodo arvore;
	return 0;
}
